version: 0.0
os: linux

files:
  - source: /WebRepo/index.html
    destination: /var/www/html
    overwrite: true
  - source: /WebRepo/style.css
    destination: /var/www/html

hooks:
  BeforeInstall:
    - location: indexscript.sh
      timeout: 300
      runas: root